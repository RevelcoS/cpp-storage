&var is a pointer, which is a memory adress, coded in hex-number.
*&var is a value from &var memory adress.
Example:
int a = 10;
int *pA = &a;
cout << "The memory adress: " << pA;
cout << "The value: " << *pA;
[[types]] [[memory adress]] [[hex-number]]